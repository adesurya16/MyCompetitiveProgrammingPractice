// dp problem https://leetcode.com/problems/target-sum/
// similar subset problem 
// using flying table mthod for optimize memory complexity

#include <bits/stdc++.h>
using namespace std;
int findTargetSumWays(vector<int>& nums, int target) {
    vector<vector<int>> dp(2, vector<int>(target + 1, 0));
    

}
int main(){
    vector<int> nums;
    nums.push_back();
    nums.push_back();
    nums.push_back();
    nums.push_back();
    nums.push_back();
    int target = ;
    cout << findTargetSumWays(nums, target) << endl;
    return 0;
}