#include <bits/stdc++.h>
using namespace std;
int main(){
	ios_base::sync_with_stdio(0), cin.tie(NULL);

    int n;
    cin >> n;
    while(n--){
        int x;
        cin >> x;
        cout << x/2 << endl;
    }
    return 0;
}